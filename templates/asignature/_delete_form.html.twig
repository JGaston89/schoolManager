<form method="post" action="{{ path('app_asignature_delete', {'id': asignature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asignature.id) }}">
    <button class="btn">Delete</button>
</form>
